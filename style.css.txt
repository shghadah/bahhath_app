body { direction: rtl; font-family: Tahoma, Arial, sans-serif; background: linear-gradient(to bottom, #d1d1d1, #999); margin:0; }
.app-container { text-align:center; padding:10px; }
.preview-btn { background:#facc15; border:none; padding:10px 20px; font-weight:bold; cursor:pointer; border-radius:30px; }
.iphone-frame { background:#fff; border:10px solid #000; border-radius:40px; width:390px; height:844px; margin:auto; overflow:hidden; position:relative; }
.screen-content { padding:20px; height: calc(100% - 86px); overflow-y:auto; }
.navbar { position:absolute; bottom:0; display:flex; flex-wrap:wrap; gap:10px 0; width:100%; justify-content:space-around; background:#f1f1f1; padding:8px; border-top:1px solid #ddd; }
.nav-item { cursor:pointer; color:gray; display:flex; flex-direction:column; align-items:center; gap:2px; font-size:12px; width: 11%; }
.nav-item.active { color:#facc15; }
.footer-text { margin-top:10px; color:#333; }
.bahhath-icon { width:50px; height:50px; background:#facc15; border-radius:50%; margin:10px auto; display:flex; align-items:center; justify-content:center; font-weight:bold; }
.card { border:1px solid #e0e0e0; border-radius:10px; padding:10px; background:#fafafa; text-align:right; margin-bottom:10px; }
.search { width:85%; padding:8px; margin:10px 0; border-radius:20px; border:1px solid #ccc; }
.grid4 { display:grid; grid-template-columns:repeat(4,1fr); gap:8px; }
.grid2 { display:grid; grid-template-columns:repeat(2,1fr); gap:8px; }
.chip { background:#f3f4f6; padding:6px 8px; border-radius:10px; display:inline-block; }
.gold { color:#9a7b00; }
.muted { color:#666; font-size:14px; }
.live-box { background:#111; color:#fff; padding:20px; border-radius:10px; }
.notes { background:#f3f4f6; border:1px solid #ddd; border-radius:8px; padding:8px; font-size:14px; height:70px; overflow:auto; }
.row { display:flex; gap:8px; align-items:center; flex-wrap:wrap; }
.profile { display:flex; gap:10px; align-items:center; margin:10px 0; }
.avatar { width:50px; height:50px; border-radius:50%; background:#ddd; }
.book-btn { background:#facc15; border:none; border-radius:20px; padding:8px 12px; cursor:pointer; font-weight:bold; }
.black-btn { background:#000; color:#fff; border:none; border-radius:20px; padding:8px 12px; cursor:pointer; }
.red-btn { background:#ef4444; color:#fff; border:none; border-radius:10px; padding:8px 12px; cursor:pointer; }
.gold-panel { background:#fff7cc; border-color:#facc15; }
.splash { display:flex; flex-direction:column; align-items:center; justify-content:center; height:100%; color:#facc15; }
.support-box { background:#eef6ff; border:1px solid #cde1ff; padding:12px; border-radius:10px; text-align:right; }
.support-actions { display:flex; gap:8px; flex-wrap:wrap; margin-top:8px; }
a.button-like { text-decoration:none; color:#000; background:#facc15; padding:8px 12px; border-radius:20px; font-weight:bold; display:inline-block; }
.success { color:#0a7f28; font-weight:600; }
